<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>epiGenesys</title>
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <meta name="author" content="Genesys Team 10">
  <meta name="description" content="This is a project platform.">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>

<nav class="navbar navbar-default navbar-fixed-top">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <button type="button" data-target="#navbarCollapse" data-toggle="collapse" class="navbar-toggle">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <a class="navbar-brand scroll" style="padding-top: 9px">
      <%= image_tag('/epiGenesys-black.png', height: '32', alt: 'epiGenesys') %>
    </a>
  </div>
  <!-- Collection of nav links and other content for toggling -->
  <div id="navbarCollapse" class="collapse navbar-collapse">
    <ul class="nav navbar-nav">
      <li><a href="/">Home</a></li>
      <% if user_signed_in? %>
        <li class="dropdown">
          <a data-toggle="dropdown" class="dropdown-toggle" href="#">Project <b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li><%= link_to("My projects", records_path, method: :get) %></li>
            <li><%= link_to("All projects", requirements_path, method: :get) %></li>
          </ul>
        </li>
        <% if current_user.admin? %>
          <li class="dropdown">
            <a data-toggle="dropdown" class="dropdown-toggle" href="#">Module <b class="caret"></b></a>
            <ul class="dropdown-menu">
              <li><%= link_to("View all modules", mods_path, method: :get) %></li>
              <li><%= link_to("View all module leaders", users_path, method: :get) %></li>
            </ul>
          </li>
        <% end %>
      <% end %>
    </ul>
    <ul class="nav navbar-nav navbar-right">
      <% if user_signed_in? %>
        <li class="dropdown">
          <a data-toggle="dropdown" class="dropdown-toggle" href="#"><strong><%= current_user.email %></strong>
            <b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li><%= link_to("My profile", records_path) %></li>
            <li class="divider"></li>
            <li><%= link_to("Logout", destroy_user_session_path, method: :delete) %></li>
          </ul>
        </li>
      <% else %>
        <li><%= link_to("Login", records_path) %></li>
      <% end %>
    </ul>
  </div>
</nav>

<main role="main" class="container" style="padding-top: 65px">
  <% if notice %>
    <div class="alert alert-success fade in">
      <a href="#" class="close" data-dismiss="alert">&times;</a>
      <%= notice %>
    </div>
  <% end %>
  <% if alert %>
    <div class="alert alert-danger fade in">
      <a href="#" class="close" data-dismiss="alert">&times;</a>
      <%= alert %>
    </div>
  <% end %>

  <%= yield %>

  <footer class="container mt-5">
    <div class="row">
      <div class="col-md-6" align="start">
        <h5>Contact</h5>
        <p><a href="tel:+441142221888">+44 (0)114 222 1888</a></p>
        <p><a href="mailto:info@epigenesys.org.uk">info@epigenesys.org.uk</a></p>
        <p>epiGenesys, Floor C, Cathedral Court<br>1 Vicar Lane, Sheffield, S1 2LT</p>
      </div>
      <div class="col-md-6" align="right">
        <%= image_tag('/uos-logo.svg', height: '64', alt: 'UoS') %>
      </div>
    </div>
  </footer>
</main>
</body>
</html>
